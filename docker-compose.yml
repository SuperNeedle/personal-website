services:
  app:
    build: .
    container_name: personal-website
    ports:
      - "8080:80"
    volumes:
      - ./.env:/var/www/html/.env:Z
    mem_limit: 512m
    restart: unless-stopped
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
